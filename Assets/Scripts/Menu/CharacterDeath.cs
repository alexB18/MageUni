using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterDeath : MonoBehaviour
{
    Subscriber deathListener = delegate (Object[] obj)
    {

    };
    // Start is called before the first frame update
    void Start()
    {
        
    }
}
